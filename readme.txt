Steps to create a repository to connect to github

1) open get bash in the folder to submit in heroku
2)git init
3)git add .
4)git commit -m 'add commemnt to project here'

it is now save and commit and now have to create a new repoisitory on github

when creating the same name repository in github 
use the same bash window added the command from git gub
starting with

"git remote add......"

then git push. 